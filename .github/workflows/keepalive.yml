name: Keep Streamlit Alive

on:
  schedule:
    - cron: "*/30 * * * *"   
  
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -s https://mini-pitch.streamlit.app/ > /dev/null


